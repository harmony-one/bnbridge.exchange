# Use postgres/example user/password credentials
version: '3'

services:
  postgres:
    image: 'postgres:latest'
    restart: always
    environment:
      POSTGRES_PASSWORD: 12345
    ports:
      - 5432:5432

